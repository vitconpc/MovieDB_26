<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.com.framgia.movie_db26">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.main.MainActivity" />
        <activity android:name=".screen.detail.DetailActivity" />
        <activity
            android:name=".screen.youtube.YoutubeActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".screen.search.SearchActivity" />
        <activity android:name=".screen.list.FindGenresActivity" />
        <activity android:name=".screen.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.cast.CastDetailActivity" />
        <activity android:name=".screen.company.CompanyActivity"/>
    </application>

</manifest>
